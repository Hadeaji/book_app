<div class="container">
    <img src="<%=bookDetails[0].image_url%>" alt="" style="width: 300px;border: 10px solid #fff;">
    <div class="dis">
        <h3><%= bookDetails[0].title %></h3>
        <h4><%=bookDetails[0].authors%></h4>
        <h4><%=bookDetails[0].description%></h4>
        <h4><%=bookDetails[0].isbn%></h4>
        <h4><%=bookDetails[0].bookshelf%></h4>


        <div id="detailsbuttons">
                <button onclick="update()">edit</button>

    
            <form action="/books/<%=bookDetails[0].id %>?_method=DELETE" method="POST" class="detailsbuttons" style="flex-direction: row;">
                
                <button type="submit" id="delete" >delete</button>
    
            </form>

        </div>
        
    </div>

    <div>
        <%- include( 'edit' ) %>
    </div>
</div>
